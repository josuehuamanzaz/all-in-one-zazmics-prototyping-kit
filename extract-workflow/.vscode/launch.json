{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Streamlit App",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/.venv/bin/streamlit", // 👈 Linux/macOS con entorno virtual
      "args": [
        "run",
        "app.py"
      ],
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
